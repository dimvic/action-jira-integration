{
  "name": "unh-jira",
  "version": "1.0.0",
  "description": "Github action for Jira integration",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild src/index.js --bundle --platform=node --target=node16 --outfile=dist/index.js"
  },
  "author": "dimvic",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.6.0",
    "@actions/github": "^5.0.0",
    "node-fetch": "^2.3.0",
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "esbuild": "^0.14.11"
  }
}
